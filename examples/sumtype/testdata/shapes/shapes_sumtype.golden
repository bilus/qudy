// Code generated by sumtype from shapes.go. DO NOT EDIT.

package shapes

// shapeVariant is the closed set of Shape's variants.
type shapeVariant interface{ isShape() }

func (Circle) isShape() {}
func (Rect) isShape()   {}

// Shape is one of its variants, and Circle when it is the zero value.
type Shape struct{ v shapeVariant }

// shapeOf constrains a type to Shape's variants.
type shapeOf interface {
	Circle | Rect
	shapeVariant
}

// NewShape wraps a variant in a Shape.
func NewShape[V shapeOf](v V) Shape { return Shape{v: v} }

// Match calls the function for the Shape's variant.
func (s Shape) Match[R any](
	onCircle func(Circle) R,
	onRect func(Rect) R,
) R {
	switch v := s.v.(type) {
	case Circle:
		return onCircle(v)
	case Rect:
		return onRect(v)
	}
	var zero Circle
	return onCircle(zero)
}

// tokenVariant is the closed set of token's variants.
type tokenVariant interface{ isToken() }

func (word) isToken()   {}
func (number) isToken() {}

// token is one of its variants, and word when it is the zero value.
type token struct{ v tokenVariant }

// tokenOf constrains a type to token's variants.
type tokenOf interface {
	word | number
	tokenVariant
}

// newToken wraps a variant in a token.
func newToken[V tokenOf](v V) token { return token{v: v} }

// Match calls the function for the token's variant.
func (s token) Match[R any](
	onWord func(word) R,
	onNumber func(number) R,
) R {
	switch v := s.v.(type) {
	case word:
		return onWord(v)
	case number:
		return onNumber(v)
	}
	var zero word
	return onWord(zero)
}
